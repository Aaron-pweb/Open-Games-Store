from .base import *

# Local development settings
DEBUG = True

# Add any additional local settings here (e.g. email backend, debug toolbar)
EMAIL_BACKEND = "django.core.mail.backends.console.EmailBackend"
